# BrokerBot Assets

Place the logo file here:

  brokerbot_logo_icon_400x100.png

The worker embeds it as base64 in HTML cards. Drop the PNG into this folder before running
The Call List routine for the first time.
